using UnityEngine;

public class Random_Math : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //print(Random.Range(0.0f, 15.0f));
        // Mathf. - a lot of math functions
    }

    // Update is called once per frame
    void Update()
    {
        //print(Random.Range(0.0f, 15.0f));
    }

    void FixedUpdate()
    {
        //print(Random.Range(0.0f, 15.0f));
    }
}
